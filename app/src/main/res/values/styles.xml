<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainSimpleButton" parent="AppTheme">
        <item name="android:background">@drawable/selecor_normal_button</item>
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_marginBottom">5dp</item>
        <!--<item name="colorButtonNormal">@color/colorPrimary</item>&lt;!&ndash;正常状态下的颜色  &ndash;&gt;-->
        <!--<item name="colorControlHighlight">@color/colorPrimaryDark</item>&lt;!&ndash;覆盖色，按下的颜色  &ndash;&gt;-->
    </style>

</resources>
